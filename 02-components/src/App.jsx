import React from 'react';

// so this App() is a component which is been export, and like these we can create custom components
export default function App() {
  return (
    <div>
      <h1>Hello, World!</h1>
    </div>
  );
}
